<h3><%= @roaster.name %></h3>
<br>
<table>
	<thead>
		<tr>
			<h4>Carried at:</h4>
		</tr>
	</thead>
	<tbody>
		<% @shop_names.each do |shop|%>
			<tr>
				<td><%= shop %></td>
			</tr>
		<% end %>
	</tbody>
</table>
<br><br>
<%= link_to "Edit Roaster", edit_roaster_path, class: "btn btn-default" %>
<%= link_to "Delete Roaster", @roaster, method: :delete,  data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %>